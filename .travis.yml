os: windows
language: csharp
mono: none
dotnet: 2.1.502
solution: Rphil.utils.sln
script:
  - choco install opencover.portable
  - dotnet restore
  - dotnet test
  - OpenCover.Console.exe -register -target:"C:/Program Files/dotnet/dotnet.exe" -targetargs:test -filter:"+[Rphil.Utils.*]*" -output:".\Rphil_Utils_coverage.xml"  -oldstyle